# 📊 Diagramas: Nginx Multi-Tenant

Visualización completa de la arquitectura y flujos de la aplicación.

---

## 🏗️ Arquitectura General

```
┌─────────────────────────────────────────────────────────────────────┐
│                           INTERNET                                   │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        DNS WILDCARD                                  │
│  adminpos.dokploy.movete.cloud → IP_DEL_SERVIDOR                    │
│  *.adminpos.dokploy.movete.cloud → IP_DEL_SERVIDOR                  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         FIREWALL (UFW)                               │
│  Puerto 80 (HTTP)  ✅                                                │
│  Puerto 443 (HTTPS) ✅                                               │
│  Puerto 22 (SSH)   ✅                                                │
│  Otros puertos     ❌                                                │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         NGINX                                        │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  Server Block 1: adminpos.dokploy.movete.cloud             │    │
│  │  (Dominio Central)                                          │    │
│  └────────────────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  Server Block 2: ~^(?<tenant>.+)\.adminpos\.dokploy\.      │    │
│  │  (Wildcard para Tenants)                                    │    │
│  └────────────────────────────────────────────────────────────┘    │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       PHP-FPM 8.2                                    │
│  Socket: /var/run/php/php8.2-fpm.sock                               │
│  Pool: www-data                                                      │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       LARAVEL 10                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Middleware: InitializeTenancyByDomain                        │  │
│  │  1. Lee dominio de la petición                                │  │
│  │  2. Busca tenant en tabla 'domains'                           │  │
│  │  3. Cambia conexión de BD                                     │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
    ┌───────────────────────┐   ┌───────────────────────┐
    │   BD Central          │   │   BDs Tenants         │
    │   pos_central         │   │   - tenanttest001     │
    │   - tenants           │   │   - tenantemp001      │
    │   - domains           │   │   - tenant...         │
    └───────────────────────┘   └───────────────────────┘
```

---

## 🔄 Flujo de Petición HTTP

### Petición al Dominio Central

```
┌─────────────┐
│  Usuario    │
│  Browser    │
└──────┬──────┘
       │
       │ 1. GET http://adminpos.dokploy.movete.cloud/login
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Archivo hosts (Local) / DNS (Producción)          │
│  adminpos.dokploy.movete.cloud → 127.0.0.1 / IP    │
└──────┬──────────────────────────────────────────────┘
       │
       │ 2. HTTP Request
       │    Host: adminpos.dokploy.movete.cloud
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Nginx - Puerto 80                                  │
│  ┌───────────────────────────────────────────────┐ │
│  │ server {                                       │ │
│  │   server_name adminpos.dokploy.movete.cloud;  │ │
│  │   root /var/www/html/public;                  │ │
│  │ }                                              │ │
│  └───────────────────────────────────────────────┘ │
└──────┬──────────────────────────────────────────────┘
       │
       │ 3. location / { try_files ... /index.php }
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Nginx → PHP-FPM                                    │
│  fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;   │
│  SCRIPT_FILENAME: /var/www/html/public/index.php   │
└──────┬──────────────────────────────────────────────┘
       │
       │ 4. Ejecuta index.php
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Laravel                                            │
│  1. Carga framework                                 │
│  2. Rutas de web.php (dominio central)              │
│  3. NO inicializa tenancy                           │
│  4. Usa BD central: pos_central                     │
└──────┬──────────────────────────────────────────────┘
       │
       │ 5. Response HTML
       │
       ▼
┌─────────────┐
│  Browser    │
│  Renderiza  │
└─────────────┘
```

### Petición a un Tenant

```
┌─────────────┐
│  Usuario    │
│  Browser    │
└──────┬──────┘
       │
       │ 1. GET http://testempresa.adminpos.dokploy.movete.cloud/dashboard
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Archivo hosts (Local) / DNS (Producción)                  │
│  testempresa.adminpos.dokploy.movete.cloud → 127.0.0.1/IP  │
└──────┬──────────────────────────────────────────────────────┘
       │
       │ 2. HTTP Request
       │    Host: testempresa.adminpos.dokploy.movete.cloud
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Nginx - Puerto 80                                          │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ server {                                               │ │
│  │   server_name ~^(?<tenant>.+)\.adminpos\.dokploy\...  │ │
│  │   # Captura: $tenant = "testempresa"                  │ │
│  │   root /var/www/html/public;                          │ │
│  │ }                                                      │ │
│  └───────────────────────────────────────────────────────┘ │
└──────┬──────────────────────────────────────────────────────┘
       │
       │ 3. location / { try_files ... /index.php }
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Nginx → PHP-FPM                                            │
│  fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;           │
│  SCRIPT_FILENAME: /var/www/html/public/index.php           │
│  TENANT_SUBDOMAIN: testempresa (extra param)                │
└──────┬──────────────────────────────────────────────────────┘
       │
       │ 4. Ejecuta index.php
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Laravel                                                    │
│  1. Carga framework                                         │
│  2. Middleware: InitializeTenancyByDomain                   │
│     ┌─────────────────────────────────────────────────┐    │
│     │ a) Lee: request()->getHost()                     │    │
│     │    → "testempresa.adminpos.dokploy.movete.cloud" │    │
│     │                                                   │    │
│     │ b) Busca en BD central (pos_central):            │    │
│     │    SELECT * FROM domains                          │    │
│     │    WHERE domain = 'testempresa.adminpos...'       │    │
│     │                                                   │    │
│     │ c) Obtiene tenant_id: "test001"                  │    │
│     │                                                   │    │
│     │ d) Cambia conexión de BD:                        │    │
│     │    DB::connection('tenant')                       │    │
│     │    BD: tenanttest001                              │    │
│     └─────────────────────────────────────────────────┘    │
│  3. Rutas de tenant.php (rutas de tenant)                   │
│  4. Todas las queries usan BD: tenanttest001                │
└──────┬──────────────────────────────────────────────────────┘
       │
       │ 5. Response HTML con datos del tenant
       │
       ▼
┌─────────────┐
│  Browser    │
│  Renderiza  │
└─────────────┘
```

---

## 🗄️ Estructura de Bases de Datos

```
                    ┌────────────────────────────────┐
                    │    BASE DE DATOS CENTRAL       │
                    │       pos_central              │
                    ├────────────────────────────────┤
                    │                                │
                    │  TABLE: tenants                │
                    │  ┌──────────────────────────┐  │
                    │  │ id   | name     | email  │  │
                    │  ├──────┼──────────┼────────┤  │
                    │  │ test001 | TestCo | t@... │  │
                    │  │ emp001  | Emp1   | e@... │  │
                    │  └──────────────────────────┘  │
                    │                                │
                    │  TABLE: domains                │
                    │  ┌────────────────────────────┐│
                    │  │ id | domain       | tenant││
                    │  ├────┼──────────────┼────────┤│
                    │  │ 1  | testempresa. | test001││
                    │  │    | adminpos...  |        ││
                    │  │ 2  | empresa1...  | emp001 ││
                    │  └────────────────────────────┘│
                    └────────────┬───────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
    ┌───────────────────────────┐  ┌───────────────────────────┐
    │ BD TENANT 1               │  │ BD TENANT 2               │
    │ tenanttest001             │  │ tenantemp001              │
    ├───────────────────────────┤  ├───────────────────────────┤
    │                           │  │                           │
    │ users                     │  │ users                     │
    │ products                  │  │ products                  │
    │ sales                     │  │ sales                     │
    │ customers                 │  │ customers                 │
    │ invoices                  │  │ invoices                  │
    │ categories                │  │ categories                │
    │ ...                       │  │ ...                       │
    │                           │  │                           │
    │ DATOS AISLADOS            │  │ DATOS AISLADOS            │
    │ TestCo Company            │  │ Emp1 Company              │
    └───────────────────────────┘  └───────────────────────────┘
```

---

## 🐳 Arquitectura Docker (Desarrollo)

```
┌─────────────────────────────────────────────────────────────────────┐
│                      docker-compose.nginx.yml                        │
└─────────────────────────────────────────────────────────────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │                      │                      │
          ▼                      ▼                      ▼
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│  Container:      │   │  Container:      │   │  Container:      │
│  nginx           │   │  php             │   │  mysql           │
├──────────────────┤   ├──────────────────┤   ├──────────────────┤
│ Imagen:          │   │ Imagen:          │   │ Imagen:          │
│ nginx:alpine     │   │ php:8.2-fpm      │   │ mysql:8.0        │
│                  │   │                  │   │                  │
│ Puerto:          │   │ Puerto:          │   │ Puerto:          │
│ 80:80            │   │ 9000 (interno)   │   │ 3306:3306        │
│                  │   │                  │   │                  │
│ Volúmenes:       │   │ Volúmenes:       │   │ Volúmenes:       │
│ - ./:/var/www/..│   │ - ./:/var/www/..│   │ - mysql-data:/..│
│ - conf/nginx/... │   │ - docker/php/... │   │                  │
│ - logs/nginx/... │   │                  │   │                  │
└────────┬─────────┘   └────────┬─────────┘   └────────┬─────────┘
         │                      │                      │
         │◄─────────────────────┤                      │
         │   FastCGI (9000)     │                      │
         │                      │◄─────────────────────┤
         │                      │   MySQL (3306)       │
         │                      │                      │
         └──────────────────────┴──────────────────────┘
                                 │
                                 ▼
                    ┌─────────────────────────┐
                    │  Network: laravel-mt    │
                    └─────────────────────────┘
```

### Servicios Adicionales

```
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│  phpmyadmin      │   │  redis           │   │  mailhog         │
├──────────────────┤   ├──────────────────┤   ├──────────────────┤
│ Puerto: 8080     │   │ Puerto: 6379     │   │ Puerto: 8025     │
│ Gestión BD       │   │ Cache/Queues     │   │ Mail Testing     │
└──────────────────┘   └──────────────────┘   └──────────────────┘
```

---

## 📁 Flujo de Archivos

### Assets Estáticos

```
Tenant: testempresa.adminpos.dokploy.movete.cloud
                    │
                    │ Request: /build/assets/app.js
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│  Nginx - Server Block Tenant                        │
│  location /build/ {                                 │
│    alias /var/www/html/public/build/;               │
│    expires 365d;                                    │
│    add_header Access-Control-Allow-Origin "*";      │
│  }                                                  │
└──────┬──────────────────────────────────────────────┘
       │
       │ Sirve directamente (sin PHP)
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  /var/www/html/public/build/assets/app.js           │
│  (Archivo físico)                                   │
└──────┬──────────────────────────────────────────────┘
       │
       │ Con headers:
       │ - Cache-Control: public, immutable
       │ - Access-Control-Allow-Origin: *
       │ - Expires: 365 days
       │
       ▼
┌─────────────┐
│  Browser    │
│  Cache      │
└─────────────┘
```

### Storage de Tenants

```
Tenant: testempresa.adminpos.dokploy.movete.cloud
                    │
                    │ Request: /storage/products/img.jpg
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│  Laravel FileSystem Tenancy Bootstrapper            │
│  storage_path() → storage/app/tenanttest001/        │
└──────┬──────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  storage/app/tenanttest001/products/img.jpg         │
│  (Archivo aislado por tenant)                       │
└──────┬──────────────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  Browser    │
└─────────────┘
```

---

## 🔐 Flujo de Autenticación

```
┌─────────────┐
│  Usuario    │
└──────┬──────┘
       │
       │ 1. Navega a: testempresa.adminpos.../login
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Laravel - Middleware Tenancy                       │
│  Identifica tenant: test001                         │
│  BD activa: tenanttest001                           │
└──────┬──────────────────────────────────────────────┘
       │
       │ 2. Muestra formulario login
       │
       ▼
┌─────────────┐
│  Usuario    │
│  Ingresa    │
│  datos      │
└──────┬──────┘
       │
       │ 3. POST /login (email, password)
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Laravel Auth                                       │
│  SELECT * FROM users                                │
│  WHERE email = '...'                                │
│  (en BD: tenanttest001)                             │
└──────┬──────────────────────────────────────────────┘
       │
       │ 4. Credenciales válidas?
       │
       ├─ SÍ ──────────────────────┐
       │                            ▼
       │                  ┌──────────────────┐
       │                  │  Crear sesión    │
       │                  │  auth()->login() │
       │                  └────────┬─────────┘
       │                           │
       │                           ▼
       │                  ┌──────────────────┐
       │                  │  Redirect        │
       │                  │  /dashboard      │
       │                  └──────────────────┘
       │
       └─ NO ─────────────────────┐
                                  ▼
                         ┌──────────────────┐
                         │  Error           │
                         │  "Invalid..."    │
                         └──────────────────┘
```

---

## 🌍 DNS Wildcard - Propagación

```
┌─────────────────────────────────────────────────────┐
│  Proveedor DNS (ej: Cloudflare, Namecheap)         │
│                                                     │
│  Zona: dokploy.movete.cloud                         │
│  ┌───────────────────────────────────────────────┐ │
│  │ Tipo A: adminpos → 123.45.67.89               │ │
│  │ Tipo A: *.adminpos → 123.45.67.89             │ │
│  └───────────────────────────────────────────────┘ │
└──────┬──────────────────────────────────────────────┘
       │
       │ Propagación DNS (24-48 horas)
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Servidores DNS Globales                            │
│  - 8.8.8.8 (Google)                                 │
│  - 1.1.1.1 (Cloudflare)                             │
│  - ISP DNS                                          │
└──────┬──────────────────────────────────────────────┘
       │
       │ Usuario consulta: testempresa.adminpos...
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Respuesta DNS                                      │
│  testempresa.adminpos.dokploy.movete.cloud          │
│  → 123.45.67.89                                     │
└──────┬──────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Servidor VPS (123.45.67.89)                        │
│  Nginx recibe petición                              │
└─────────────────────────────────────────────────────┘
```

---

## 📊 Comparación Arquitecturas

### Multi-Tenant (Actual)

```
               NGINX
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
 Tenant1      Tenant2      Tenant3
    │            │            │
    └────────────┼────────────┘
                 │
            MISMO CÓDIGO
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
   BD1          BD2          BD3

Ventajas:
✅ Un solo código
✅ Updates centralizados
✅ Menos recursos
✅ Escalable
```

### Multi-Instance (Tradicional)

```
  NGINX1       NGINX2       NGINX3
    │            │            │
    ▼            ▼            ▼
 Código1      Código2      Código3
    │            │            │
    ▼            ▼            ▼
   BD1          BD2          BD3

Desventajas:
❌ Código duplicado
❌ Updates manuales × N
❌ Más recursos
❌ Difícil de escalar
```

---

## 🔄 Ciclo de Vida de una Petición (Timeline)

```
Tiempo | Proceso
-------|----------------------------------------------------------------
0 ms   | Usuario hace clic en "http://testempresa.adminpos.../products"
5 ms   | DNS resuelve a IP del servidor
10 ms  | Conexión TCP establecida
15 ms  | Nginx recibe petición HTTP
       | - Lee header Host
       | - Matchea con server_name regex
       | - Identifica server block correcto
20 ms  | Nginx intenta servir archivo estático
       | - /products no existe como archivo
       | - try_files falla
       | - Reescribe a /index.php?/products
25 ms  | Nginx pasa a PHP-FPM (socket Unix)
30 ms  | PHP-FPM inicia worker
35 ms  | Laravel bootstrap
       | - Carga autoloader
       | - Inicia application
       | - Carga service providers
50 ms  | Middleware stack ejecuta
       | - InitializeTenancyByDomain
       |   · Lee host: testempresa.adminpos...
       |   · Query BD central: SELECT domain...
       |   · Identifica tenant: test001
       |   · Cambia conexión BD: tenanttest001
80 ms  | Router encuentra ruta
       | - Route::get('/products', ProductController@index)
85 ms  | Controller ejecuta
       | - $products = Product::all();
       | - Query: SELECT * FROM products (BD: tenanttest001)
95 ms  | Query completada
       | - Resultados obtenidos
100 ms | View renderiza
       | - Blade compila template
       | - Genera HTML
150 ms | Response HTML completa
       | - Headers HTTP
       | - Contenido HTML
155 ms | Nginx envía respuesta al browser
160 ms | Browser recibe HTML
       | - Parsea DOM
       | - Solicita assets (CSS, JS, imágenes)
165 ms | Nginx sirve assets (directamente, sin PHP)
       | - /build/assets/app.css
       | - /build/assets/app.js
200 ms | Browser renderiza página completa
       | - CSS aplicado
       | - JavaScript ejecutado
-------|----------------------------------------------------------------
TOTAL: ~200ms para página completa
```

---

¿Necesitas más diagramas? Consulta la [documentación completa](COMO_FUNCIONA_NGINX.md).


