# ============================================
# Configuración Nginx Multi-Tenant - Desarrollo Local
# ============================================
# Archivo: nginx-multitenant-local.conf
# Uso: Para desarrollo local en Windows con Docker o WSL

# Servidor para dominio central (sin subdominios)
server {
    listen 80;
    server_name adminpos.dokploy.movete.cloud;
    
    root C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/public;
    index index.php index.html index.htm;
    
    charset utf-8;
    
    # Logs específicos para dominio central
    access_log C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/storage/logs/nginx-central-access.log;
    error_log C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/storage/logs/nginx-central-error.log;
    
    # Headers de seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    
    # Bloquear acceso a archivos sensibles
    location ~ /\.(?!well-known).* {
        deny all;
    }
    
    location /.git {
        deny all;
        return 403;
    }
    
    # Favicon y robots
    location = /favicon.ico { 
        access_log off; 
        log_not_found off; 
    }
    
    location = /robots.txt  { 
        access_log off; 
        log_not_found off; 
    }
    
    # Assets estáticos con cache
    location ~* \.(jpg|jpeg|gif|png|css|js|ico|webp|tiff|ttf|svg|woff|woff2|eot)$ {
        expires 365d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }
    
    # Directorio build (Vite assets)
    location /build/ {
        alias C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/public/build/;
        expires 365d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }
    
    # Directorio vendor
    location /vendor/ {
        alias C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/public/vendor/;
        expires 365d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }
    
    # Storage público
    location /storage/ {
        alias C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/storage/app/public/;
        expires 7d;
        add_header Cache-Control "public";
    }
    
    # Ruta principal - Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    # Manejo de PHP
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        include fastcgi_params;
        
        # Aumentar timeouts para procesos largos
        fastcgi_read_timeout 300;
        fastcgi_send_timeout 300;
        
        # Buffer settings
        fastcgi_buffer_size 128k;
        fastcgi_buffers 256 16k;
        fastcgi_busy_buffers_size 256k;
        fastcgi_temp_file_write_size 256k;
    }
    
    # Denegar acceso a archivos PHP en storage
    location ~* ^/storage/.*\.php$ {
        deny all;
    }
}

# Servidor wildcard para TODOS los subdominios (tenants)
server {
    listen 80;
    # Wildcard: captura cualquier subdominio
    server_name ~^(?<tenant>.+)\.adminpos\.dokploy\.movete\.cloud$;
    
    root C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/public;
    index index.php index.html index.htm;
    
    charset utf-8;
    
    # Logs específicos para tenants
    access_log C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/storage/logs/nginx-tenants-access.log;
    error_log C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/storage/logs/nginx-tenants-error.log;
    
    # Headers de seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    
    # Pasar el subdominio como variable a PHP (útil para debugging)
    fastcgi_param TENANT_SUBDOMAIN $tenant;
    
    # Bloquear acceso a archivos sensibles
    location ~ /\.(?!well-known).* {
        deny all;
    }
    
    location /.git {
        deny all;
        return 403;
    }
    
    # Favicon y robots
    location = /favicon.ico { 
        access_log off; 
        log_not_found off; 
    }
    
    location = /robots.txt  { 
        access_log off; 
        log_not_found off; 
    }
    
    # Assets estáticos con cache
    location ~* \.(jpg|jpeg|gif|png|css|js|ico|webp|tiff|ttf|svg|woff|woff2|eot)$ {
        expires 365d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }
    
    # Directorio build (Vite assets)
    # Los tenants cargan assets desde el dominio central
    location /build/ {
        alias C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/public/build/;
        expires 365d;
        add_header Cache-Control "public, no-transform";
        add_header Access-Control-Allow-Origin "*";
        access_log off;
    }
    
    # Directorio vendor
    location /vendor/ {
        alias C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/public/vendor/;
        expires 365d;
        add_header Cache-Control "public, no-transform";
        add_header Access-Control-Allow-Origin "*";
        access_log off;
    }
    
    # Storage por tenant
    location /storage/ {
        alias C:/Users/USUARIO/Documents/proyecto-pos/app-pos-laravel/storage/app/public/;
        expires 7d;
        add_header Cache-Control "public";
    }
    
    # Ruta principal - Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    # Manejo de PHP
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        include fastcgi_params;
        
        # Aumentar timeouts para procesos largos
        fastcgi_read_timeout 300;
        fastcgi_send_timeout 300;
        
        # Buffer settings
        fastcgi_buffer_size 128k;
        fastcgi_buffers 256 16k;
        fastcgi_busy_buffers_size 256k;
        fastcgi_temp_file_write_size 256k;
    }
    
    # Denegar acceso a archivos PHP en storage
    location ~* ^/storage/.*\.php$ {
        deny all;
    }
}


